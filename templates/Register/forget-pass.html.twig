{% extends 'base.html.twig' %}
{% block title %}Réinitialisation du mot de passe{% endblock %}

{% block content %}
    <div class="m-5">
        <h2 class="text-center m-auto mb-5">Mot de passe oublié ?</h2>
    </div>

    <div class="col-lg-6 m-auto mb-100" >
        <div  class="card card-small mb-4">
            <div class="card-header border-bottom">
                <h6 class="m-0">Entrez votre adresse email pour mettre à jour vos identifiants.</h6>
            </div>
            <ul class="list-group list-group-flush">
                <li class="list-group-item p-3">
                    <div class="row">
                        <div class="col">
                            {{ form_start(form) }}
                            <div class="form-group">
                                {{ form_row(form.email) }}
                            </div>
                            <div class="form-group text-center">
                                <button type="submit" class="btn btn-success mt-3">Réinitialiser mon mot de passe</button>
                            </div>
                            {{ form_end(form) }}
                        </div>
                    </div>
                </li>
            </ul>
        </div>
    </div>
{% endblock %}
